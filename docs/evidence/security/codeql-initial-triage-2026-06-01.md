# CodeQL Initial Triage - 2026-06-01

Issue: #273.

Basis: `gh api repos/Sapientropic/AIppocampus/code-scanning/alerts` on
2026-06-01, after `main` `8311010` (`Split runtime core helper owners (#324)`).

Canonical context checked:

- `SECURITY.md`
- `docs/guides/privacy-security-checklist.md`
- `docs/guides/public-core-boundary.md`
- `docs/guides/release-checklist.md`

This file is an evidence log for the initial CodeQL pass, not a new privacy
contract. The source docs above still own the public/private boundary.

## Current State

- Open alerts: 55
- Fixed alerts: 27
- Dismissed alerts: 0
- CodeQL remains enabled through default GitHub code scanning.

Already fixed by earlier work:

- `#1`, `#2`: `actions/missing-workflow-permissions`
- `#13`-`#16`: weak-hash findings in legacy/top-level scripts
- `#24`-`#26`, `#34`-`#38`, `#46`-`#48`, `#52`-`#57`: logging findings in
  legacy/top-level scripts or already-hardened runtime surfaces
- `#64`, `#69`-`#71`: storage findings in legacy/top-level scripts

## Open Alert Dispositions

| Alerts | Rule | Primary surface | Disposition | Action |
| --- | --- | --- | --- | --- |
| `#3` | `js/xss-through-dom` | `dashboard_v2.js:84` | Real DOM boundary. Pane bodies are generated by trusted Python code, but the browser still read JSON text and reinterpreted it as HTML. | Fixed in this branch by escaping JSON script HTML delimiters, escaping dynamic health counts, emitting structured `body_nodes`, and rendering those nodes through escaped markup instead of parsing pane body HTML with `template.innerHTML`. Leave the GitHub alert open until the PR CodeQL scan confirms closure. |
| `#4`-`#12`, `#17`-`#20`, `#123`, `#124` | `py/weak-sensitive-data-hashing` | cache keys, ambient cards/policy/cache, registry ids, source ids, conversation source ids, workspace/thread fingerprints | Mixed. Most are stable identifiers rather than password hashing, but changing them blindly can break source-backed indexes and public contracts. | Split follow-up: migrate low-risk cache/lock/job fingerprints to SHA-256 or BLAKE2s; design a compatibility plan before changing source/thread/workspace ids. Do not bulk-dismiss. |
| `#22`, `#23`, `#27`-`#33`, `#39`-`#45`, `#58`-`#62` | `py/clear-text-logging-sensitive-data` | live confirmation, subconscious jobs/worker/scheduler, prompt hook debug log, semantic gate CLI | Real or near-real privacy risk. Several CLI/debug paths can print prompt previews, aliases, findings, reasons, or raw JSON payloads. | Split follow-up: default to summary-only output, require explicit raw/private switches, and keep debug logs redacted. |
| `#49`-`#51` | `py/clear-text-logging-sensitive-data` | `smoke_semantic_scope_real_history.py` | Real public-output boundary. The internal smoke result may carry compact model-route/cache/error diagnostics, so CLI `--json` should not serialize it directly. | Fixed in the #329 follow-up by projecting CLI output through an aggregate-only public result. Leave the alerts open until PR CodeQL confirms closure. |
| `#63`, `#65`-`#68`, `#74` | `py/clear-text-storage-sensitive-data` | ambient policy/events, job findings, hook debug log, warm ambient artifacts, worker output | Local-first private artifacts are allowed, but CodeQL is correctly forcing the project to document/gate raw storage surfaces. | Split follow-up: mark local-private artifact writers, redact or summarize default payloads where user-visible, and keep raw audit data behind explicit local storage boundaries. |
| `#72`, `#73` | `py/clear-text-storage-sensitive-data` | test fixtures | Acceptable test fixtures for redaction/storage behavior. | Dismiss with rationale after review; do not remove tests that protect the redaction boundary. |
| `#75`-`#80` | `py/path-injection` | `tests/aippocampus/test_object_storage_sync.py` | Test HTTP object server false positive. The path is first checked by `validate_relative_sync_path`, which rejects absolute paths, drives, empty paths, and `..`; writes stay under a temp bucket root. | Dismiss with source-context rationale after review. No production path change needed from this alert alone. |

## Follow-Up Cut Lines

1. Raw/private CLI output hardening:
   `subconscious/jobs.py`, `subconscious/worker.py`,
   `subconscious/scheduler.py`, `question/confirmation_live.py`,
   `hooks/prompt.py`, and `recall/semantic_recall_gate.py`.

2. Local-private storage policy:
   `subconscious/job_storage.py`, warm ambient stores, prompt hook logs, and
   ambient policy events.

3. Hash-id migration:
   separate cache-only fingerprints from source-backed ids before changing
   algorithms. Cache keys can move cheaply; public/source ids need migration
   compatibility.

4. Source-context dismissals:
   test fixtures, test object-server path writes, and aggregate-only smoke
   output should be dismissed only after the rationale is attached to the
   exact alert or encoded as a stable suppression mechanism.

## Non-Claims

- #273 remains open for the remaining CodeQL follow-up buckets.
- The 55 open alerts are not all false positives.
- Passing tests does not replace CodeQL closure; the alert list must be checked
  again after GitHub scans the PR.

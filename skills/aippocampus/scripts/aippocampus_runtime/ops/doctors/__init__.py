"""Provider, preflight, and spend doctor owner package.

New doctor surfaces belong here. The legacy flat modules
`aippocampus_runtime.ops.provider_doctor` and
`aippocampus_runtime.ops.spend_doctor` remain public import wrappers until the
next public-API review can retire direct module entrypoints.
"""

"""Provider-aware onboarding package for AIppocampus.

The top-level `onboard*.py` files remain script/import compatibility shims.
Runtime and facade code should use this package so onboarding state, status,
and optional frontier extraction have one implementation owner.
"""

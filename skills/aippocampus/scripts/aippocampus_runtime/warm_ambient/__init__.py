"""Warm ambient recall helper package.

This package owns the warm-path ambient recall runner plus detached scheduling,
prompt rendering, scout taxonomy, and source-ref validation helpers. Runtime
entrypoints should call the package module (`aippocampus_runtime.warm_ambient.cli`)
instead of relying on legacy top-level shims.
"""

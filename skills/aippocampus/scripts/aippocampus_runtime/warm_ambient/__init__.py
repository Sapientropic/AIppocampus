"""Warm ambient recall helper package.

This package owns the warm-path ambient recall runner plus detached scheduling,
prompt rendering, scout taxonomy, and source-ref validation helpers. The
top-level `warm_ambient_recall.py` and `ambient_warm_scheduler.py` files remain
only as compatibility shims so installed and scheduled entrypoints do not fork a
second implementation.
"""

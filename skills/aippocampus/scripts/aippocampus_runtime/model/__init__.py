"""External-model helpers for AIppocampus runtime jobs.

The top-level `model_client.py` and `deepseek_model_routing.py` modules remain
compatibility shims. New runtime imports should use this package so provider
capabilities and chat-client transport have one owner.
"""


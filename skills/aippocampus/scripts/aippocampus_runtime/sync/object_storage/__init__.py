"""Object-storage transport helpers for sync.

The object-storage sync command lives in
``aippocampus_runtime.sync.object_storage.cli``. The top-level
``sync_object_storage.py`` script remains a compatibility shim.
"""

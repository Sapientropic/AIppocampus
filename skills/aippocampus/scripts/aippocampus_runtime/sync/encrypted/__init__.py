"""Encrypted sync package owner.

Top-level `encrypted_sync_*.py` modules remain compatibility shims and public
script entrypoints. Keep reusable encrypted sync implementation here so future
object-provider and migration changes do not grow the flat runtime directory.
"""

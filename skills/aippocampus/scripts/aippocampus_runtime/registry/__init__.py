"""Registry helper package.

Registry CLI behavior is owned by ``aippocampus_runtime.registry.api`` and the
public ``aippocampus`` facade. Shared storage, search, and provider-key helpers
live here so future registry work has one package owner.
"""

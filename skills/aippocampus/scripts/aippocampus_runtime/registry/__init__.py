"""Registry helper package.

The public `registry.py` command stays at the runtime root for documented CLI
compatibility. Shared storage, search, and provider-key helpers live here so
future registry work has one package owner instead of growing the flat script
directory.
"""


interface:
  display_name: "AIppocampus"
  short_description: "Orient early with source-backed conversation continuity"
  default_prompt: "Start by pulling the smallest useful recall route with $aippocampus when prior source could change the next move; deepen the selected route before exact, sensitive, stale, conflicting, or high-risk claims. If no AIppocampus tool is visible, use the CLI facade or surface the install/update card instead of guessing from memory."

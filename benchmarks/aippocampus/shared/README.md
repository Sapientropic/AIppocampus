# Shared Benchmark Helpers

Reusable benchmark helpers live here when they do not own a public benchmark
command. Keep this layer deterministic and importable from root-level runners.

Examples: statistics, maturity labels, source-claim boundary references,
source-window diagnostics, Stage 3 runtime projections, sampling helpers, and
provider budget checks.

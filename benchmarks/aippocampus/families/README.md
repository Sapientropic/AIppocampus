# Benchmark Family Helpers

Family-specific helpers live here when they are too coupled to one benchmark
family to belong in `shared/`, but too large or reusable to stay inside a
single runner.

Examples: hippocampal fixture schemas, continuous-memory preregistered slices,
and LongMemEval answer-review helpers.

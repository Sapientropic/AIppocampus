# Benchmark Adapters

Adapters translate public benchmark/provider surfaces into AIppocampus-safe
inputs and reports. They should not decide benchmark pass/fail policy; runner
scripts and shared helpers own that boundary.
